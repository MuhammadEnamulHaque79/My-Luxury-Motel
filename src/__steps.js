// firebase setup steps;

/*
1.Create a project into firebase;
2.Register App;
3.npm install firebase(yarn add firebase);
4.initialized firebase into firebase.init.js and const auth=getAuth(app)and const export default auth;
5.Authentication and enable desire provider(like email/password and google);
6.


*/

// react-router setup steps;

/*
1.yarn add react-router-dom@6(install into cmd);
2.import {BrowserRouter}from 'react-router-dom' and wrap app with it;
3.Open App.js and import {Routes,Route} from 'react-router-dom';
4.

 */

// Activites in hole project;

/*
1.Create a pages folder into it create Home folder and into it create Home component(like Home.js & Home.css);
2.Create a Shared folder into it create Header folder and into it create Header component(like Header.js & Header.css);
3.

 */

// Framework using steps(like react-bootstrap);

/*
1.npm install react-bootstrap bootstrap@5.1.3 and import 'bootstrap/dist/css/bootstrap.min.css' into index.js;
2.
 */


// firebase hosting steps;

/*
1.npm install -g firebase-tools;
2.firebase login;
3.firebase init;
4.firebase build;
5.firebase deploy;
 */
// Following above steps you can nicely complete a website(like educational,ecommerce,hotel,motel etc);